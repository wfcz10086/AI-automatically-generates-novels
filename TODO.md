# TODO

> 更新于 2026-09-07。测试 82 项单测 + 27 项 E2E 全绿。

## 下一轮候选

### 1. LLM-as-Judge 裁判（优先级最高）
现有三层质检都是规则 + 统计，能抓「写了什么不该写的」，抓不住「写得好不好」。
缺一个换厂模型做主观打分（开头钩子 / 人设立体 / 节奏 / 爽感），并入回归基准。
> 依据：本轮多次出现「机器分 100 但读起来平」的情况，规则分已经到顶了。

### 2. 跨项目风格指纹去重
同一模型写不同的书会复用同一批比喻与开场方式。已有的口癖沉淀是 per-book 的，
跨书不共享 —— 本轮才把「才刚刚开始」这类万金油提升为全局默认黑名单，
但比喻和场景调度的指纹还没治。

### 3. 多线并行叙事
现在是单主线推进。长篇需要主线 / 支线交替，配角独立线程。
需要在卷层增加线程标记，并在细纲阶段分配镜头。
> 依据：全书体检里「主角戏份垄断」「配角纸片化」是所有书的共性问题。

### 4. 多项目并发生成
现在一次只能跑一本（flock 互斥）。工作室场景需要并行多本，
且共享检索缓存与事实卡。

### 5. 思维导图（总纲 / 卷结构可视化）
老版 `mind.js`（1291 行）未迁移。用途：总纲与分卷结构的可视化编辑，
比纯文本大纲更适合梳理主线支线。

### 6. 完本后的伏笔清算模式
实测一本完本长篇仍有 159 条未回收伏笔。需要一个收尾模式：
把未回收伏笔按重要度排序，在末卷集中兑现或显式废弃。

---

## 已落地

### 架构：四层可插拔
- Provider（模型网关）/ ContentType（4 种）/ GenrePack（23 个）/ Exporter
- **检索也是 provider**（`providers/search.py`），与模型网关并列，配在同一份 yaml
- **自带 SearXNG**：docker-compose + settings.yml + setup.sh，装完即用；不可用自动降级
- 响应适配器：11 种厂商返回形状（content / reasoning_content / reasoning / thinking / …）
- **两类思考开关约定**：toggle（可关）与 effort（关不掉只能调档 low/high/max）
- **四种内容类型都真的跑得起来**：成品层取 `levels[-1]` 而非 id 白名单
  （动漫的 storyboard 曾不在白名单里，这个类型从来就崩）；非小说类型走
  `compose_typed_prompt`，类型包管格式，记忆/角色卡/禁用词/去 AI 味纪律照常共享

### 世界基底：七种预设 + 通用基底卡
- `real` 真实历史 / `modern` 当代现实 / `alt` 架空 / `fanfic` 同人 /
  `mythos` 神话体系 / `multiworld` 多世界 / `invented` 纯虚构
- 映射由**题材包声明**，不再硬编码在引擎里；穿越 / 武侠 / 军事 / 系统流 / 无限流
  标记为修饰性题材，新建时提示手动确认
- **七种是预设不是穷举**：建项目时让模型读设定自己判定基底并写出红线清单，
  存 `basis.md`（可编辑、可重新判定）。红线进约束层，检测词进验收黑名单
- 穿帮检测按「有没有检测词/时代锚点」开关，不按模式名 ——
  曾经 2005 年的都市重生书穿帮检测完全关闭

### 记忆与检索
- 分层记忆 L0–L5 + 预算分配器，32k–100k 自动推导，逐层用量可视化
- 统一召回：内部 FTS5 + 外部检索合并，查过的知识点全书复用
- **检索式由大模型生成**：正则只抓 1 条废话，模型给 5 条精准检索式
- **全流程背景落地**：世界观 / 人物关系 / 章节剧情 / 正文四处都先查真实背景
- **超长内容压缩而非截断**：`condense()` 保留首尾 + 中段抽样 + 标注省略量
- **本书资产整份传**：总纲 / 世界观 / 角色档案 / 时代卡 / 基底卡不截断
- 知识库 `${kb}`：用户自填 + 检索事实卡；事实卡可查看来源、逐条删除、主动检索

### 长程一致性：八类动态台账
不可逆事实 canon / 伏笔 / 身份变更 / 势力架构 / 角色状态 / 实力地位 / 资源账 / 时间线
- **台账口径由题材包决定**：修仙记「灵石与资源 / 修为境界」，历史记「钱粮与军资 /
  官职与兵权」，都市记「资金台账 / 身份与资源」
- 实力账进约束层时带「只进不退，不得凭空跃升或倒退」
- 角色卡带「当前身份（第 N 章起，以此为准）」—— 档案记出身，台账记现状
- 全部台账在「记忆」页逐条可看、可筛选、可删除

### 提示词体系：六层合成
通用（`anti_ai_rules` + `chapter_directives`）/ 类型 / 题材 / 文风 / 本书 / 记忆
- 19 个变量由**服务端**按项目填充（曾经前端把 `${cliche_blacklist}` 替换成空串）
- 内置模板全文可在「提示词」页展开、一键载入编辑；留空表示继承内置改进
- 内置模板不含模式专属措辞，有回归测试扫描

### 质量
- **质检三层**：单章 → 邻章窗口 → 全书，三者结论全部回流到下一章约束
- **自我改进循环**：每 5 章自读自批，产出《本书写作守则》注入后续章节
- **AI 味密度检测**：套话词根正则、对白节奏切碎、状态复述、动作修饰连挂
- **字数硬闸门**：低于文风包下限必须扩写（不是重写）；要价按交付率补偿，验收线不动
- 伏笔自动抽取与回收追踪；超 15 章未回收进约束告急
- 冷启动口癖防护 + 跨书通用口癖全局黑名单

### 界面
- 心跳徽标：命令行长跑的进度界面自动跟上，不必手动刷新
- 工作台首屏 683ms → 70ms（字数缓存）
- 静态资源指纹：改了 JS 不用教用户硬刷新
- 目录接口失败给可操作错误页而非白屏
- 右键改写覆盖正文 / 大纲 / 世界观 / 人物档案 / 每章细纲；按下标精确替换

### 工具
- `scripts/reflow.py`：已写章节重泡（清元数据 + 扩写到规格 + 重新评分）
- `scripts/build_release.py`：按平台规格重切章（番茄 2000-3200 字/章）
- `scripts/compare.py`：两本书的指标对比

---

## 本轮修掉的典型缺陷

| 缺陷 | 危害 |
|---|---|
| 花名册解析只认一种标题格式 | 14 个角色只解析出 1 个，配角卡从不注入 |
| 本书年号被判成穿帮词 | 全书 42 处合法用法算违规 |
| 评审无史实维度 | 「主角在焚书坑儒时…」三遍评审全放过 |
| 抽取只读章节前 4000 字 | 8683 字的章后半段伏笔全丢 |
| FTS5 只索引前 1500 字 | 远程召回对后半章失效 |
| 分卷只看总纲前 1600 字 | 排卷时看不到结局与节奏表 |
| 必守约束整块拼两遍 | 白烧上万 token |
| 苏婉儿同时「本章出场」与「不得再提」 | 提示词自相矛盾 |
| 字数只写 47% 仍得 87 分 | 重写闸门不触发 |
| 界面不自动刷新 | 页面停在打开时的快照 |
